"use client";

const viewUserButton = () => {
    contst handleClick = () => alert('Udah Di Klik')
    return (
    <>
    <button onClick={() => console.log("lihat user")}>lihat user</button>
    </>
    );
};